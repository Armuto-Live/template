import { Button,Grid,styled } from '@mui/material'

export const CustomButton=styled(Button)((theme)=>({
    color:"black"
}))

export const NavbarContainer=styled(Grid)((theme)=>({
    boxShadow:"0 .5rem .3rem rgba(0,0,0,0.2)",
}))